var React = require('react');

var kpi = React.createClass({
    render: function () {
        return(
            <div className="kpi">
                Ich bin eine KPI
            </div>
        );
    }
});

module.exports = kpi;